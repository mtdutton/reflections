When would you want to use a remote repository rather than keeping all your work local?

  One key benefit to using a remote repository would be so that you could share the
  repository with other developers.  Space limitations on a local harddrive could
  come into play as well.
  
